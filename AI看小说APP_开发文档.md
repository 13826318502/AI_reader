# AI 看小说 App 开发文档

版本：v1.0

文档用途：本文件可以直接提供给 Agent，作为产品、交互、数据结构和开发验收依据。

## 1. 项目概述

### 1.1 产品定位

开发一个以“沉浸式阅读 + AI 世界观理解 + AI 可控创作”为核心的移动端小说阅读 App。

它不是普通的网络小说平台，也不以登录、社交和商业化为第一目标，而是服务于当前写作 App 的个人阅读端：

- 写作 App 负责创建和维护小说、章节、角色、世界观、组织、关系图谱、大纲、伏笔等源数据。
- 阅读 App 负责展示已经发布的小说、提供沉浸式阅读、保存阅读进度，并基于当前小说上下文提供 AI 阅读能力。
- 两个 App 之间通过统一数据协议互通，而不是直接共享或修改对方的 SQLite 文件。
- 阅读过程中生成的 AI 内容默认属于“阅读分支/AI 番外”，不能静默覆盖原作。

### 1.2 已确认的产品约束

以下要求必须遵守：

- 不增加“写作目标”功能。
- 写作 App 只需要支持导出 TXT，不增加其他复杂导出格式作为首期要求。
- 不增加登录场景。
- 首期以本地优先为主，不强制建设账号系统和云端后端。
- 用户可以配置自己的文字 AI 和图片 AI 服务；API Key 不得明文写入作品导出包。
- 当前需求是编写开发文档，不是立即开发本 App。

## 2. 现有项目状态

### 2.1 当前 `D:\writer` 的状态

当前工作区已经不是之前被删除的旧 React Native 项目，而是重新建立的 Flutter Android MVP，产品名称为“弧光｜移动端 AI 小说创作工作室”。

当前项目特征：

- Flutter Android 项目，包名为 `com.arcglow.writer`。
- 主要业务代码集中在 `D:\writer\lib\main.dart`。
- 当前已经有作品管理、项目主页、章节列表、章节编辑、角色、世界观、大纲、伏笔、知识库、关系图谱、AI 助手、灵感和设置等入口。
- 已经使用 SQLite 保存项目、章节、暂存变更、条目和设置。
- 已经有 JSON 归档能力，以及 TXT 导出方向。
- 已经验证可以构建 Android APK 并安装到手机上运行。
- 当前界面仍然偏“写作工作台”，不是阅读产品；没有完整的书架、沉浸式阅读器、阅读进度、书签、批注、读者端发布快照等能力。
- 当前没有 AI 图片生成入口，也没有封面、角色卡、场景图的生图资产管理。
- 当前没有阅读过程中手动输入需求生成图片，并将图片作为新页面插入阅读流的能力。

### 2.2 当前项目的技术风险

这些问题不阻止编写阅读 App，但在两个 App 真正互通前必须处理：

1. 当前 MVP 的完整业务数据表还没有覆盖 CharacterArc 规划中的全部数据模型。
2. 一些角色、世界观、大纲等模块仍有演示数据或简化实现，不能直接视为稳定数据源。
3. 当前 AI API Key 仍保存在本地设置表中，正式版本必须迁移到安全存储。
4. 当前项目没有稳定的跨 App 数据交换协议，不能让阅读 App 直接读取写作 App 的数据库文件。
5. 当前关系图谱、章节版本、完整数据库迁移和云同步仍不是生产级完成状态。

### 2.3 CharacterArc 的最终产品方向

`D:\下载` 下没有名为 `characterArc` 的文件夹，但有两份与该项目直接相关的说明文件：

- `D:\下载\CharacterArc_移动端APP开发说明.md`
- `D:\下载\CharacterArc_移动端能力提前实现方案.md`

CharacterArc 的最终方向不是单纯的章节编辑器，而是一个“小说创作世界操作系统”，核心数据包括：

- 多项目管理。
- 角色卡、组织、人物关系和组织成员。
- 世界观条目和分类。
- 大纲卷、节点和章节。
- 伏笔、情节线、知识库和参考资料。
- AI 助手、章节助手、暂存区和可审阅变更。
- 世界状态数据库和一致性检查。
- 封面工作室、AI 生图、批量生成和风格预设。
- TXT 导入/导出、作品续写、章节拆分合并和本地/WebDAV 数据同步。

因此，AI 看小说 App 应当被设计为 CharacterArc 的“读者端/体验端”，而不是重新复制一套孤立的数据结构。

## 3. 市场参考与设计结论

本产品不复制任何单一 App，而是吸收专业阅读产品已经验证的能力：

### 3.1 微信读书的参考点

微信读书强调相对安静的阅读体验，并提供书架、划线、笔记、阅读统计、书籍问答、听书等能力。公开信息中也能看到其书籍问答、阅读进度、笔记和听书方向：[微信读书 App Store 页面](https://apps.apple.com/ca/app/%E5%BE%AE%E4%BF%A1%E8%AF%BB%E4%B9%A6/id952059546?platform=vision)、[微信读书公开能力说明](https://github.com/Tencent/WeChatReading/blob/main/README.md)。

本项目吸收：

- 干净的阅读页。
- 书签、划线、批注和阅读进度。
- “向这本书提问”的 AI 入口。
- 统一的个人书架。

### 3.2 起点读书和 QQ 阅读的参考点

起点读书强调原创内容、书架、推荐、读者互动和章节评论；QQ 阅读强调纸张感阅读、自动阅读和听书：[起点读书官方页面](https://www.yuewen.com/app/?type=appqd)、[QQ 阅读官方页面](https://www.yuewen.com/app?type=appqq)。

本项目吸收：

- 书架中的封面、进度和最近阅读。
- 目录、卷章结构和章节导航。
- 可选的自动滚屏和 TTS。
- 将评论/互动改造成更适合个人作品的“阅读笔记”和“世界观问答”，首期不引入公开社区。

### 3.3 对起点读书当前产品的具体借鉴

根据起点读书官方产品页和 App Store 当前公开信息，起点读书目前的产品重点包括：定制书架、更新同步、人工及智能推荐、书单、书友互动、舒适阅读、书签与笔记、云端同步、自动阅读以及 AI/真人听读等。[起点读书官方产品页](https://www.yuewen.com/app/?type=appqd)、[起点读书 App Store 页面](https://apps.apple.com/cn/app/%E8%B5%B7%E7%82%B9%E8%AF%BB%E4%B9%A6-%E7%9C%8B%E5%B0%8F%E8%AF%B4%E5%90%AC%E4%B9%A6%E7%9A%84%E9%98%85%E8%AF%BB%E7%A5%9E%E5%99%A8/id534174796)。

本项目的对应设计如下：

| 起点读书的成熟能力 | 本项目的实现方式 |
| --- | --- |
| 定制书架 | 本地书架、标签、分组、网格/列表切换 |
| 推荐和书单 | 首期改为“我的作品/已发布世界/最近导入”，后续再做个人推荐 |
| 更新同步 | 写作端发布快照，阅读端显示新版本并由用户确认升级 |
| 阅读进度、书签、笔记 | 本地优先保存，精确到章节和段落锚点 |
| 自动阅读和 AI 听读 | 预留 TTS、自动滚屏和进度跟随能力 |
| 章节评论、书友互动 | 首期改为私人批注、AI 问答和世界观探索，不做公开社区 |
| 书城和活动 | 首期不做付费书城、活动中心和广告，保持小说阅读主流程纯净 |

起点当前版本历史还显示了“AI 听读与文字阅读进度同步”“自动阅读时查看段评”“本地书导入方式优化”等方向。这说明阅读进度同步、听读联动和本地导入值得放入本项目的数据层设计，但不必在第一版全部完成。

### 3.4 番茄小说的参考点

番茄小说公开强调免费阅读、个性化推荐、分类阅读和 AI/语音听书：[番茄小说官方页面](https://fanqienovel.com/app_download)。

本项目吸收：

- 内容分类和本地书架管理。
- 适合移动端的快速恢复阅读。
- TTS 作为后续能力。

本项目不直接复制其内容混排和强运营模式。书架必须以小说为中心，短剧、广告、排行榜和社交信息不应打断阅读主流程。

### 3.5 设计原则

- 阅读优先：阅读页面不堆叠工具按钮。
- 内容不被 AI 静默改写：原作、AI 图片页和用户笔记必须有明确边界。
- 世界观可探索：角色、组织、地点和设定应能从正文中快速打开。
- 本地优先：没有登录也能完成导入、阅读、进度保存和 AI 配置。
- 可回退：所有 AI 生成结果都可以取消、删除、隐藏或恢复。
- 视觉统一：写作端和阅读端共享“弧光”品牌，但阅读端更安静、更像书。

### 3.6 根据手机实际点击确认的功能范围

已在连接的 Android 手机中打开起点读书，并实际查看了作品详情、目录、书签、足迹、正文阅读和阅读设置页面。起点的本地阅读结构可以借鉴，但以下功能明确排除：

排除：

- 书友圈、段评、公开评论和社区互动。
- 作者主页、作者互动和公开分享。
- 同类推荐、榜单、精品频道和运营活动。
- 充值、订阅、月票、打赏和其他付费流程。
- 必须联网才能完成的公共书城、云端推荐和社区同步。

保留并适配到本项目：

- 作品详情页。
- 目录、章节列表、最近阅读位置和进度。
- 书签、足迹和阅读笔记。
- 正文沉浸式阅读。
- 亮度、字号、字体、背景主题、护眼/夜间模式。
- 边距、行距、翻页方式、自动阅读和阅读显示设置。
- 本地 TXT/文档导入、导入后直接阅读和本地作品管理。
- AI 手动生图、AI 图片页插入和世界百科。
- AI 封面、角色卡等图片生成入口；实际调用可在配置服务后进行。

实际观察到的起点阅读页还将章节、阅读进度、目录入口和阅读设置集中在清晰的阅读工具栏中。本项目沿用这种结构，但用“AI 阅读”替换社区入口，用“世界百科”连接角色、世界设定和关系图谱。

## 4. 产品信息架构

### 4.1 底部导航

首期使用四个主入口，阅读器本身使用全屏页面，不长期占用底部导航：

1. **书架**：最近阅读、全部作品、分组、进度。
2. **发现**：本地已发布作品、从写作端导入的作品、世界观入口。首期不做公共内容平台。
3. **AI 阅读**：手动输入图片需求、生成图片、管理已插入图片页。
4. **我的**：阅读设置、图片 AI 配置、文字 AI 配置、数据导入导出、关于。

### 4.2 页面树

```text
App
├─ 书架
│  ├─ 最近阅读
│  ├─ 全部作品
│  ├─ 分组/标签
│  ├─ 作品详情
│  └─ 书架管理
├─ 发现
│  ├─ 已发布世界
│  ├─ 作品详情
│  └─ 世界百科
├─ 阅读器
│  ├─ 目录
│  ├─ 书签/划线/笔记
│  ├─ 本章 AI 问答
│  ├─ AI 图片页
│  └─ 阅读设置
├─ AI 阅读
│  ├─ 当前作品问答
│  ├─ 角色视角
│  ├─ 世界观解释
│  └─ AI 图片页历史
└─ 我的
   ├─ AI 服务配置
   │  ├─ 文字模型
   │  └─ 图片模型
   ├─ 阅读偏好
   ├─ 数据管理
   └─ 关于
```

## 5. 视觉与交互规范

### 5.1 整体风格

沿用 CharacterArc 的“深夜书房 / 弧光”方向，但阅读端减少管理面板感：

- 深色背景：`#0E1016`。
- 深色卡片：`#151924`、`#1C2230`。
- 主文字：`#E8E6E0`。
- 辅助文字：`#9CA3B3`。
- 主强调色：琥珀 `#E8B64C`。
- AI 强调色：紫色或蓝紫色，避免与删除按钮的红色混淆。
- 亮色纸张主题：`#F5F1E8`，正文使用深灰而不是纯黑。

### 5.2 字体和阅读排版

- 书名、卷名和章节标题优先使用 Noto Serif SC 或系统衬线字体。
- 功能按钮和设置页面使用 Noto Sans SC 或系统无衬线字体。
- 正文默认 18sp，可调节到 16–24sp。
- 正文行高 1.8–2.0。
- 左右边距默认 20dp，可调整。
- 段落之间使用首行缩进或段间距二选一，不能同时过度使用。
- 支持系统字体放大，不因字体放大导致按钮或弹窗无法操作。

### 5.3 触控和可访问性

- 所有主要点击区域不小于 44dp。
- 图标必须有语义化无障碍标签。
- 阅读页支持单手操作：右侧轻触下一页、左侧轻触上一页，中间轻触呼出工具栏。
- 不用颜色作为唯一状态提示；AI 图片页、书签和草稿需要同时使用文字/图标标识。
- 支持 TalkBack，支持横屏阅读作为后续增强项。

## 6. 核心功能需求

## 6.1 书架

### 必须实现

- 显示封面、书名、作者标识、当前章节、阅读百分比和最近阅读时间。
- 支持列表/网格切换。
- 支持按最近阅读、更新时间、名称排序。
- 支持标签和分组。
- 支持删除书架记录，但删除前必须区分“移出书架”和“删除本地作品”。
- 支持从写作 App 导入已发布作品包。
- 支持空状态页面，引导导入作品或打开写作 App。

### 作品卡片操作

- 继续阅读。
- 查看作品详情。
- 查看世界百科。
- 重新导入最新本地文件。
- 移出书架。
- 删除本地作品数据。

### 作品详情

显示：

- 封面、书名、简介、类型、字数、章节数。
- 发布版本和更新时间。
- 继续阅读按钮。
- 目录。
- 作品导入文件信息。
- 本地阅读统计。

### 新增：作品详情页功能

作品详情页是书架卡片和沉浸式阅读器之间的承接页面，也是写作 App 跳转到阅读 App 时的默认落点。它不能只显示封面和简介，而要帮助用户快速判断“现在读什么、读到哪里、还能探索什么”。

#### 进入方式

- 点击书架中的作品卡片。
- 写作 App 点击“在阅读 App 中打开作品”。
- 通过深链打开作品但没有指定章节时。
- 从世界百科或 AI 图片页记录返回作品。

#### 页面结构

从上到下包含：

1. 顶部返回、更多操作和版本状态。
2. 作品封面、书名、类型、标签和发布版本。
3. 简介和作品基本信息：总字数、章节数、最近更新时间、最近阅读时间。
4. 主操作“继续阅读”。
5. 阅读进度卡：当前卷章、百分比、预计剩余时间、查看目录。
6. 导入文件信息：文件名、文件类型、导入时间和当前本地版本。
7. 次要操作：加入书架、重新导入本地文件、导出 TXT、打开写作端。

#### 交互要求

- “继续阅读”必须是页面唯一最强主按钮。
- 用户没有阅读进度时，按钮显示“开始阅读”；有进度时显示“继续阅读”。
- 点击进度卡中的“查看目录”进入目录，并定位到最近阅读章节。
- 书签、笔记、字体、主题、自动阅读、阅读设置和 AI 生图不在详情页选择，统一在正文阅读页的控制面板中选择。
- 需要查看角色、世界设定或关系图谱时，先进入正文阅读页，再从阅读控制面板打开“世界百科”。
- 点击“打开写作端”通过深链跳回对应项目；写作端未安装时显示作品包导出选项。
- 点击更多操作可以移出书架、删除本地作品数据、查看导入版本、重新导入本地文件、导出 TXT，但不能删除写作端源项目。

#### 状态设计

详情页至少覆盖以下状态：

- 无封面：显示可读的渐变占位和“生成封面”入口。
- 未开始阅读：显示“开始阅读”。
- 阅读中：显示章节、百分比和继续阅读。
- 有新版本：显示“发现新发布版本”，用户确认后更新。
- 本地文件已导入：显示文件来源、导入时间和当前作品版本。
- 正在导入文件：显示解析进度，不阻塞已经导入作品的阅读。
- 文件损坏或格式不支持：显示原因和重新导入入口。

#### 与当前写作端界面的衔接

当前手机上的写作端已经形成了稳定的视觉语言：米白背景、琥珀色小标题、深色信息卡、圆角模块和大字号中文标题。阅读端作品详情页应沿用这些设计 token，但把写作端的“工作台信息”转换成“作品阅读信息”：

- 保留米白背景和琥珀色主按钮。
- 用真实封面替代当前写作端列表里的文字渐变缩略图。
- 详情页只展示作品和文件信息，不放正文阅读工具。
- 将阅读工具集中到正文阅读页底部控制面板，避免详情页和阅读页职责混淆。
- 减少工作台式的空白，把可操作信息集中在首屏和首屏下方。

详情页的阅读入口布局参考手机实际查看的起点详情页截图；本次重新生成的完整流程目标图见 `D:\reader\UI目标稿_完整阅读流程总览.png`。

#### 作品详情页验收标准

- [ ] 从书架点击作品可以进入详情页。
- [ ] 详情页可以显示封面、书名、简介、字数、章节数、版本和进度。
- [ ] 点击“继续阅读”能打开正确章节和段落锚点。
- [ ] 点击目录和重新导入本地文件能进入对应流程。
- [ ] 无封面、无进度、文件损坏和有新版本状态都有明确界面。
- [ ] 详情页不出现书签、笔记、字体、主题、自动阅读和阅读设置操作。
- [ ] 作品详情页不会把阅读端操作误认为写作端编辑操作。
- [ ] 从写作 App 深链进入时，可以根据传入参数直接打开作品详情或指定章节。

## 6.2 沉浸式阅读器

### 阅读页面

阅读页面必须保持简洁，只保留必要的阅读操作：

- 章节标题。
- 正文内容。
- 当前阅读进度。
- 页面/章节导航。
- 轻触呼出的工具栏。

工具栏提供：

- 目录。
- 上一章/下一章。
- 书签。
- 划线和笔记。
- 搜索本章/全书。
- 本章 AI 生图。
- 新建图片页并插入当前阅读位置。
- 阅读设置。

### 阅读控制面板的位置

所有阅读过程中的选择都在正文阅读页完成。用户点击正文中央后，从底部展开控制面板；再次点击正文后收起，不离开当前章节。

第一行固定提供：

- 目录。
- 书签/笔记。
- 字体/主题。
- 阅读设置。

第二行提供：

- 自动阅读。
- 上一章。
- 章节进度滑杆。
- 下一章。

第三行提供可选能力：

- AI 生图：手动输入画面需求，可引用当前段落、角色卡和世界设定。
- 新建图片页：确认生成结果后，在当前阅读位置插入独立图片页。
- 世界百科：角色、地点、世界设定和关系图谱。

这些操作不应出现在作品详情页的固定卡片中。正文阅读页和完整流程布局见 `D:\reader\UI目标稿_完整阅读流程总览.png`。

### 阅读设置

- 深色、浅色、纸张、护眼主题。
- 字号、字体、行距、段距、页边距。
- 滚动阅读/分页阅读。
- 自动滚屏速度。
- 屏幕常亮。
- 横屏阅读。
- TTS/自动朗读入口，作为后续实现但从数据结构上预留。

### 进度和本地文件

- 每次翻页或滚动停留后自动保存进度。
- 进度至少精确到章节和段落锚点，不能只保存百分比。
- 正文直接从已经导入的 TXT/文档或作品包读取，不额外设计“离线缓存”功能。
- AI 生图和 AI 图片页属于可选联网能力；这些服务不可用时，不影响本地作品阅读。

## 6.3 世界百科与角色卡

阅读过程中可以打开“世界百科”，查看写作 App 发布的结构化资料：

- 角色卡：姓名、别名、身份、外貌、性格、能力、当前状态、关系。
- 组织卡：名称、性质、成员、目标、阵营。
- 地点卡：名称、位置、环境、关联事件。
- 世界观条目：规则、历史、力量体系、专有名词。
- 关系图谱：人物之间的关系，支持缩放和点击节点。

### 正文关联

如果章节段落包含已发布实体的稳定 ID，阅读器可以显示轻微的可点击标识。没有稳定 ID 时不得强行猜测并修改正文；可以提供“搜索百科”作为备用入口。

### 内容版本原则

- 阅读端只读取写作端的“已发布快照”。
- 未发布的角色、设定和章节不能出现在读者端。
- 读者端可以保存自己的笔记，但不能覆盖写作端的源数据。
- 发布新版本后，用户可以继续阅读旧版本，也可以升级到新版本；首期默认保留旧版本阅读进度。

## 6.4 AI 阅读中的手动生图

### 入口

- 正文阅读页底部控制面板中的“AI 生图”。
- 角色卡中的“生成角色图”。
- 世界百科地点/场景条目中的“生成场景图”。

### 手动输入

用户必须可以直接输入自己的画面需求，不能只依赖系统自动生成提示词。输入框示例：

`雨夜的山海渡口，林青瓷撑伞站在灯下，远处有雾中的古塔，电影感，横构图。`

生成面板提供：

- 自由文本提示词输入。
- “引用当前段落”开关，默认关闭或由用户主动打开。
- 引用角色卡/地点卡/世界设定的多选项。
- 角色/场景/封面/插图类型。
- 图片比例、风格、画面质量和负面提示词。
- 生成图片按钮。

### 图片页插入

生成结果确认后提供两个明确操作：

- `保存到素材库`：只保存图片，不改变正文。
- `新建一页并插入`：在当前段落锚点之后新增一个独立图片页。

图片页字段至少包含：

- `pageId`、`bookId`、`chapterId`、`anchor`。
- 图片资源 ID。
- 用户提示词和模型信息摘要。
- 页面标题，可选。
- 是否显示、创建时间和所属阅读版本。

插入后的图片页必须显示“AI 生成图片页”标签，并支持移动位置、隐藏、删除和重新生成。它不能伪装成原作正文，也不能覆盖原始 TXT 内容。

## 7. AI 图片生成系统

这是区别于普通阅读 App 的核心功能之一。

## 7.1 图片 AI 配置入口

路径：`我的 → AI 服务配置 → 图片生成`。

### 配置字段

- 配置名称，例如“本地 SDXL”“通义万相”“自定义图片服务”。
- 服务商类型。
- API Base URL。
- API Key。
- 模型名称。
- 默认尺寸和比例。
- 生成质量/步数。
- 默认风格预设。
- 默认负面提示词。
- 并发数和超时。
- 是否允许在移动网络下生成。

### 服务适配层

必须使用统一的 `ImageGenerationProvider` 接口，不把某一家服务商的字段写死在页面中。

首期建议支持：

- OpenAI 兼容图片接口。
- 阿里云通义万相等国内图片服务。
- 自定义 HTTP 图片服务。

阿里云公开文档将通义万相列为文字生成图像和人像风格重绘服务，可作为国内服务适配器的参考：[阿里云 AI 服务选择说明](https://help.aliyun.com/zh/decision-guides/how-to-choose-ai-service)。

### 安全要求

- API Key 使用 Android Keystore 对应的安全存储方案保存。
- API Key 不进入 TXT、JSON 作品包、截图、日志和 AI 生成记录导出文件。
- 日志只能记录服务商、模型、耗时、状态和错误类别，不能记录完整 Key。
- 提供“测试连接”按钮，测试失败要显示可读错误。
- 作品导入到另一台设备时，图片服务配置不自动复制 Key，只复制非敏感的模型配置。

## 7.2 图片生成场景

### 封面

入口：作品详情 → 封面 → AI 生成封面。

默认提示词由以下内容组成：

- 作品名和类型。
- 作品简介。
- 世界观关键词。
- 用户指定的构图、色调和风格。
- 负面提示词。

用户可以在生成前编辑完整提示词，并选择 1–4 张候选图。

生成后支持：

- 预览。
- 重试。
- 保存到作品素材库。
- 设为当前封面。
- 删除候选图。
- 查看使用的模型和提示词摘要。

### 角色卡

入口：世界百科 → 角色 → 角色卡 → AI 生图。

角色卡可以选择：

- 半身像。
- 全身像。
- 立绘。
- 多套服装。
- 不同年龄阶段。
- 表情变化。

角色生成提示词应优先读取角色卡中的姓名、年龄、身份、外貌、服饰、性格和所属世界观。用户可以锁定种子或引用已有角色图来保持角色一致性；如果服务商不支持参考图，界面要明确说明不能保证完全一致。

### 场景图和插页配图

为后续扩展预留：

- 地点卡 → 生成地点概念图。
- 章节 → 生成章节关键场景图。
- 图片页 → 生成或替换图片。

首期可以只实现封面和角色卡，但数据结构不能限制后续资产类型。

## 7.3 图片任务状态

每个生成任务至少有以下状态：

`queued`、`running`、`succeeded`、`failed`、`cancelled`。

必须支持：

- 任务进度或轮询状态。
- 取消。
- 失败重试。
- 多候选结果。
- 生成记录。
- 生成结果保存到本地素材库。
- 图片与作品/角色/章节的关联。

## 8. 阅读中 AI 生图并新建图片页

这是本产品的核心差异化流程：用户在正文阅读页手动输入画面需求，生成图片，确认后新建一个图片页并插入当前阅读位置。

### 8.1 用户流程

```text
正文阅读页点击“AI 生图”
        ↓
手动输入画面需求
        ↓
选择是否引用当前段落、角色卡和世界设定
        ↓
选择比例/风格/类型
        ↓
点击“生成图片”
        ↓
预览、重试或保存
        ↓
点击“新建一页并插入”
        ↓
图片页出现在当前段落之后
```

### 8.2 图片页显示规则

- 图片页与原作正文有明显分隔。
- 页面顶部显示“AI 生成图片页”。
- 可显示用户输入的提示词摘要和生成时间。
- 图片页默认只属于当前阅读端和当前作品版本。
- 图片页不能修改原始 TXT/文档正文。
- 用户可以隐藏、删除、移动位置或重新生成。
- 目录中可以将图片页显示为当前章节下的子项。

### 8.3 生成结果操作

- `保存到素材库`：保存图片，但不插入正文阅读流。
- `新建一页并插入`：创建图片页并插入当前锚点之后。
- `重新生成`：保留原结果，生成新的候选图。
- `编辑提示词`：返回输入框修改用户需求。
- `删除`：删除图片页关联，但不删除原作章节。

### 8.4 与写作 App 的关系

- 阅读端图片页默认不回写写作端正式正文。
- 用户可以将图片页导出为写作端暂存素材或草稿包。
- 写作端必须经过用户审核后，才能决定是否把图片页或图片素材用于正式章节。

## 9. 两个 App 的数据互通

## 9.1 互通原则

不要让阅读 App 直接打开 `D:\writer` 的 SQLite 数据库，也不要让两个 App 同时写同一个数据库文件。

推荐采用：

1. 写作 App 生成“已发布作品快照”。
2. 快照使用版本化 JSON 数据包，并可附带正文 TXT 和图片资源。
3. 阅读 App 导入快照，写入自己的本地数据库。
4. 写作 App 和阅读 App 使用稳定 UUID 关联相同的项目、章节、角色和世界观实体。
5. 阅读 App 的 AI 图片页和笔记使用自己的数据表；需要回写时生成新的草稿包。

## 9.2 `ArcWorldBundle v1` 建议结构

```json
{
  "format": "ArcWorldBundle",
  "version": 1,
  "bundleId": "uuid",
  "projectId": "uuid",
  "snapshotId": "uuid",
  "revision": 12,
  "exportedAt": "2026-08-24T00:00:00Z",
  "sourceApp": "arc-writer",
  "project": {},
  "chapters": [],
  "characters": [],
  "organizations": [],
  "relationships": [],
  "worldviewEntries": [],
  "outline": [],
  "plotThreads": [],
  "assets": [],
  "manifest": {}
}
```

### 必须有的字段

所有实体至少包含：

- `id`：稳定 UUID。
- `projectId`：所属作品。
- `revision`：实体版本。
- `updatedAt`：更新时间。
- `status`：草稿、已发布、已归档等。
- `source`：实体来源。

章节至少包含：

- `chapterId`。
- `volumeId`。
- `chapterNumber`。
- `title`。
- `content` 或正文文件引用。
- `paragraphAnchors`。
- `publishedRevision`。

## 9.3 导入和同步方式

首期支持三种方式，优先级如下：

1. **同设备直接跳转和导入**：写作 App 调用 Android Intent/Deep Link 打开阅读 App。
2. **本地文件导入**：通过系统文件选择器导入 `.arcworld` 或 `.zip` 作品包。
3. **二维码/配对码**：写作 App 显示短期配对码，阅读 App 扫描后传输作品包；首期可以只实现文件和深链，二维码作为后续增强。

不要求登录。云同步或跨设备同步可以在以后通过 WebDAV 或自建后端增加。

## 9.4 App 间直接跳转

写作 App 点击“在阅读 App 中打开”时，传递：

- `projectId`。
- `snapshotId`。
- `chapterId`。
- `paragraphAnchor`，可选。
- `mode`：继续阅读、打开章节、打开角色、打开世界百科。

建议支持：

- Android 自定义 Scheme，例如 `arc-reader://open/project/{projectId}/chapter/{chapterId}`。
- Android App Links，后续可以使用 HTTPS 链接。
- 阅读 App 未安装时，写作 App 显示导出/安装提示，而不是无响应。

## 10. 推荐数据结构

### 10.1 阅读端核心表

```text
reader_books
- id
- project_id
- snapshot_id
- title
- cover_asset_id
- local_path
- imported_at
- last_opened_at

reader_progress
- id
- book_id
- chapter_id
- paragraph_anchor
- percent
- updated_at

reader_bookmarks
- id
- book_id
- chapter_id
- paragraph_anchor
- title
- created_at

reader_highlights
- id
- book_id
- chapter_id
- start_anchor
- end_anchor
- color
- note_id

reader_notes
- id
- book_id
- chapter_id
- anchor
- content
- created_at
- updated_at

ai_image_pages
- id
- book_id
- snapshot_id
- chapter_id
- anchor
- type
- content
- status
- is_visible
- model_name
- prompt_summary
- created_at
- updated_at

image_assets
- id
- project_id
- owner_type
- owner_id
- asset_type
- local_path
- remote_url_optional
- width
- height
- prompt_summary
- created_at

image_generation_profiles
- id
- name
- provider_type
- base_url
- model
- settings_json
- secret_ref
- enabled

image_generation_runs
- id
- profile_id
- asset_type
- owner_id
- request_json_without_secret
- status
- error_message
- created_at
- finished_at
```

### 10.2 数据隔离

- 写作端数据库和阅读端数据库分开。
- 作品包是跨 App 的交换格式。
- 图片实体引用文件路径或资源 ID，不在正文中嵌入大段 Base64。
- API Key 只保存到安全存储，并通过 `secret_ref` 关联。
- AI 图片页、笔记、书签和阅读进度属于阅读端用户数据。

## 11. 技术方案

### 11.1 客户端

- 使用 Flutter，与当前 `D:\writer` 技术栈保持一致。
- 状态管理建议使用 Riverpod；如果为了复用当前 MVP 代码，也可以首期继续使用轻量 Provider，但新模块不得把全部状态继续堆积到单一 `main.dart`。
- 本地数据库首期可继续使用 `sqflite`，正式扩展建议使用 Drift 或拆分清晰的 Repository 层。
- 阅读排版可以使用 Flutter 原生文本渲染或 Markdown/富文本渲染组件，但正文必须支持段落锚点。
- 图片缓存使用本地文件缓存，不把大图直接塞进 SQLite。

### 11.2 AI 服务

- 文字生成支持 OpenAI 兼容接口和自定义 REST 接口。
- 文字流式输出使用 SSE 或兼容的流式协议。
- 图片生成统一走 Provider Adapter。
- 图片任务如果服务商是异步任务，使用“提交任务 → 轮询 → 下载 → 保存到本地素材库”的流程。
- 所有 AI 调用保存最小化日志，不保存不必要的完整正文和敏感配置。

### 11.3 是否建设后端

首期不强制建设后端。纯本地方案可以完成：

- 作品导入。
- 导入后的本地阅读。
- 阅读进度。
- 书签、批注和笔记。
- 用户自己的文字 AI 和图片 AI 调用。
- AI 图片页。

后续只有在需要以下能力时才增加后端：

- 多设备自动同步。
- 统一的公共书库。
- 服务器代调用模型。
- 长时间图片/文字任务队列。
- 公共评论、推荐和社区。

## 12. 分阶段开发计划

### Phase 0：数据协议和工程拆分

目标：让写作 App 能够稳定发布作品快照。

- 定义 `ArcWorldBundle v1`。
- 为项目、章节、角色、世界观、组织、关系图谱建立稳定 UUID。
- 写作端增加“发布到阅读端”或“导出作品包”。
- 统一正文段落锚点。
- 将 API Key 迁移到安全存储。
- 不修改现有写作目标和登录要求。

验收：一个写作项目可以导出包含正文、角色、世界观和关系数据的作品包，并能在另一处校验包结构。

### Phase 1：阅读 MVP

- Flutter 阅读 App 工程。
- 书架。
- 作品包导入。
- 作品详情。
- 目录和章节阅读。
- 阅读进度。
- 书签、划线和笔记。
- 深色/浅色/纸张主题。
- 从写作 App 深链打开指定章节。
- 导入后的本地阅读。

验收：不登录、不联网时，用户仍可导入和阅读已经存在的作品。

### Phase 2：世界观互通和 AI 图片

- 世界百科。
- 角色、组织、地点和关系图谱。
- 文字 AI 配置。
- 图片 AI 配置入口。
- 封面生成。
- 角色卡生成。
- 图片素材库和任务记录。
- 模型配置测试、失败重试和本地素材保存。

验收：用户可以配置一个图片模型，生成封面和角色卡，并在对应作品/角色页面使用生成结果。

### Phase 3：阅读中 AI 生图与新建图片页

- 阅读页“AI 生图”入口。
- 手动输入画面需求。
- 可选引用当前段落、角色卡和世界设定。
- 选择角色/场景、比例、风格和质量。
- 生成图片、预览、重试和保存素材。
- “新建一页并插入”到当前章节锚点之后。
- 图片页显示/隐藏、移动、删除和重新生成。
- 导出为写作端暂存素材或草稿包。

验收：图片生成不会覆盖原作；用户确认后才能创建独立图片页，并且图片页可以随时隐藏、移动或删除。

### Phase 4：增强能力

- TTS 和自动朗读。
- 作品内语义检索和 RAG。
- 更完整的一致性检查。
- 跨设备 WebDAV 同步。
- AI 图片批量生成。
- 章节场景图和插页配图。
- 可选公共书库、推荐和评论。

## 13. Agent 开发执行要求

Agent 开始开发前必须先做以下事情：

1. 检查当前 `D:\writer` 的代码、截图、`开发报告.md` 和 CharacterArc 两份设计文档。
2. 不删除当前写作 App，不覆盖用户已有数据。
3. 先输出数据协议和页面路由，再实现 UI。
4. 不把阅读端和写作端做成一个含糊的超级页面；两端应保持职责清晰。
5. 不增加登录流程。
6. 不增加写作目标功能。
7. 写作端导出首期只保证 TXT 和作品交换包；不要把 PDF、Word、Markdown 作为首期必做功能。
8. 所有 AI 生图入口都必须先允许用户配置服务商和模型。
9. 任何 AI 生成内容都要有状态、来源、模型和时间记录。
10. 任何会改变原作的操作都必须经过用户确认。
11. 不把 API Key 写进源码、作品包、日志或截图。
12. 每完成一个阶段，都要在真实 Android 手机上验证，不只验证模拟器。

## 14. 验收标准

### 阅读基础

- [ ] 不登录也能进入 App。
- [ ] 可以导入 `ArcWorldBundle v1`。
- [ ] 作品出现在书架。
- [ ] 可以打开已经导入本地的章节。
- [ ] 关闭并重新打开 App 后阅读进度仍然存在。
- [ ] 可以添加、查看和删除书签、划线和笔记。
- [ ] 可以修改字号、行距、主题和阅读方式。

### 两端互通

- [ ] 写作端可以发布或导出作品快照。
- [ ] 阅读端可以识别项目、章节、角色、世界观和关系的稳定 ID。
- [ ] 写作端可以直接跳转到阅读端的指定作品/章节。
- [ ] 阅读端未安装时有明确提示。
- [ ] 新发布版本不会无提示覆盖旧版本阅读状态。

### AI 生图

- [ ] 可以新增、编辑、删除图片模型配置。
- [ ] API Key 使用安全存储。
- [ ] 可以测试图片服务连接。
- [ ] 可以生成封面候选图。
- [ ] 可以生成角色卡候选图。
- [ ] 可以保存、替换和删除图片资产。
- [ ] 失败任务可以重试，运行任务可以取消。

### AI 图片页

- [ ] 阅读页可以打开“AI 生图”。
- [ ] 用户可以手动输入图片需求。
- [ ] 可以选择是否引用当前段落、角色卡和世界设定。
- [ ] 可以选择图片类型、比例和风格。
- [ ] 可以预览、重试、保存或删除生成结果。
- [ ] 可以点击“新建一页并插入”。
- [ ] 图片页与原作正文有清晰视觉区分。
- [ ] 图片页可以隐藏、移动和删除。
- [ ] 需要回写时只能进入写作端暂存区，不能直接改正式正文。

## 15. 最终产品判断

这个方向可以实现，而且当前 `D:\writer` 已经具备一部分写作端基础。真正的关键不是马上给阅读 App 增加页面，而是先建立稳定的 `ArcWorldBundle` 发布协议和实体 ID。完成这一步后，写作端与阅读端可以在不登录、不共享数据库、不破坏原作的前提下互通。

推荐的产品顺序是：

```text
CharacterArc 写作端
        ↓ 发布快照
ArcWorldBundle v1
        ↓ 导入/深链
AI 阅读端
        ├─ 沉浸式阅读
        ├─ 世界百科
        ├─ 封面/角色卡 AI 生图
        └─ 阅读中 AI 图片页
```

## 16. 参考资料

- [微信读书 App Store 页面](https://apps.apple.com/ca/app/%E5%BE%AE%E4%BF%A1%E8%AF%BB%E4%B9%A6/id952059546?platform=vision)
- [微信读书公开能力说明](https://github.com/Tencent/WeChatReading/blob/main/README.md)
- [起点读书官方 App 页面](https://www.yuewen.com/app/?type=appqd)
- [QQ 阅读官方 App 页面](https://www.yuewen.com/app?type=appqq)
- [番茄小说官方下载页面](https://fanqienovel.com/app_download)
- [阿里云 AI 服务选择说明](https://help.aliyun.com/zh/decision-guides/how-to-choose-ai-service)
- [当前写作端开发报告](D:/writer/开发报告.md)
- [CharacterArc 移动端开发说明](D:/下载/CharacterArc_移动端APP开发说明.md)
- [CharacterArc 移动端能力提前实现方案](D:/下载/CharacterArc_移动端能力提前实现方案.md)
