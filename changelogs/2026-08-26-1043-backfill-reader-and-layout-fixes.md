# 阅读器真实数据链路与界面稳定性补记

- 日期：2026-08-26 10:43
- Agent：Codex
- 说明：补记此前已完成但未及时写入 changelog 的改动。

## 做了什么

- 移除书架、作品详情、搜索、阅读器、设定集和角色页面中的静态演示数据。
- 实现多作品导入、章节解析、持久化保存，以及书架、阅读器、搜索和设定集之间的数据同步。
- 增加书架长按操作、AI 图片分类、小说封面同步和阅读设置持久化。
- 修复设定集标题横向溢出、阅读器正文分页溢出和页面主题重建导致的 Flutter 依赖树断言。
- 阅读器改为固定分页，不使用纵向滚动，并缩小单页正文容量以适配手机屏幕。
- 忽略 Android 构建产生的 `.hprof` 临时文件。

## 更改的文件

- `lib/main.dart`：真实作品数据链路、阅读器分页、封面/图库同步、页面布局与生命周期修复。
- `.gitignore`：忽略 `.hprof` 构建临时文件。

## 验证

- `flutter analyze`：无编译错误。
- `flutter test`：全部测试通过。
- 已在 PLS120 真机上启动验证书架、设定集和阅读器页面。
